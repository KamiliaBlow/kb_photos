--local Core = exports.vorp_core:GetCore()

-- RegisterNetEvent('vorp_camera:processPhoto')
-- AddEventHandler('vorp_camera:processPhoto', function(photoUrl)
    -- local src = source
    -- local User = Core.getUser(src)

    -- if not User then
        -- return 
    -- end

    -- local Character = User.getUsedCharacter

    -- local meta = {
        -- description = "Img: " .. photoUrl,
        -- link = photoUrl,
        -- type = "photo",
        -- date = os.date("%d.%m.%Y %H:%M")
    -- }

    -- exports.vorp_inventory:addItem(src, Config.PhotoItemName, 1, meta)
-- end)

-- Core.Callback.Register('vorp_camera:usePhoto', function(source, cb, data)
    -- local meta = data.meta
-- end)